<%= render 'refinery/admin/search_header', :url => refinery.productions_admin_productions_path %>
<div class='pagination_container'>
  <% if @productions.any? %>
    <%= render 'productions' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
